package com.austinactivities;

import org.junit.Before;
import org.junit.Test;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import com.austinactivities.Interests.*;

import static org.junit.Assert.*;

public class RecommendedActivitiesTest {
   /* List<Interests> sampleList;

    @Before
    public void setUp() {
        sampleList = new ArrayList<>(Arrays.asList(Interests.Water_Activity, Interests.Museums, Interests.Live_Music,
                Interests.Food_Trucks, Interests.Restaurants));
    }

    @Test
    public void testGenerateActivityListSwitchStatement() {
        List<Activity> sample = RecommendedActivities.generateActivityList(sampleList);
        System.out.println(sample.toString());

        sampleList = new ArrayList<>(Arrays.asList(Interests.Restaurants, Interests.Food_Trucks, Interests.Live_Music));
        List<Activity> sample2 = RecommendedActivities.generateActivityList(sampleList);
        System.out.println("Second set of samples: ");
        System.out.println(sample2.toString());

    }
    @Test
    public void testGenerateActivityListValidOutputs() {
        List<Activity> sample = RecommendedActivities.generateActivityList(sampleList);
        for (Activity a : sample) {
            System.out.println(a);
        }
    }

    @Test
    public void testGenerateActivityListSwitchStatementReturnsCutDownList() {
        List<Activity> sample = RecommendedActivities.generateActivityList(sampleList);
        assertNotNull(sample);
        assertEquals(3, sample.size());
        for(Activity a : sample) {
            System.out.println(a);
        }
    }*/
}